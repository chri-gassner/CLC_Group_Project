# -----------------------------------------------------------------------------
# Common runtime config (SAFE TO COMMIT)
# Copy to env/common.env and fill the blanks.
# -----------------------------------------------------------------------------
# Cloud Run endpoint (fill in after deployment)
INGEST_URL=


GOOGLE_CLOUD_PROJECT=clc-group-vision-2026
GCP_REGION=europe-west3

# Path inside container (safe, not a secret)
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/gcp_sa.json

PYTHONUNBUFFERED=1
PYTHONIOENCODING=utf-8
LANG=C.UTF-8
LC_ALL=C.UTF-8

VIDEO_DIR=/app/src/data
MANIFEST=/output/manifest.csv
